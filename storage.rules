rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and are only for development purposes.
    // DO NOT USE IN PRODUCTION.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}